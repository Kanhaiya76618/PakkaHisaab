# Intake fixtures

`vision_khaata.json` and `vision_invoice.json` are **PLACEHOLDER** fixtures created
on 2026-07-26 because this repository has no sample images. They match the §7.1 intake
JSON schema but were not recorded from a live model call. Replace them by recording
the corresponding prompt once images and a usable `OPENAI_API_KEY` are available.
