# Runtime dependencies for the FastAPI service. Mirrors backend/pyproject.toml.
# Railpack detects a requirements.txt far more reliably than a setuptools pyproject,
# and the deploy contract test keeps the two in step.
fastapi>=0.115,<1.0
httpx>=0.27,<1.0
openai>=1.0,<2.0
Pillow>=10.0,<12.0
PyJWT>=2.10,<3.0
python-multipart>=0.0.9,<1.0
reportlab>=4.0,<6.0
uvicorn[standard]>=0.30,<1.0
